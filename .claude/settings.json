{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; if echo \"$f\" | grep -qE '\\.(ts|tsx)$'; then npx eslint --fix \"$f\" && npx prettier --write \"$f\"; fi; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Running lint & format..."
          }
        ]
      }
    ]
  }
}
